# 蝦皮廣告代操儀表板 — 完整使用說明書

> 版本：v3.4
> 適用對象：代操業者、操作員、被授權的客戶

---

## 目錄

1. [系統概覽](#1-系統概覽)
2. [登入與帳號](#2-登入與帳號)
3. [介面導覽](#3-介面導覽)
4. [每日操作流程](#4-每日操作流程)
5. [CSV 上傳完整指南](#5-csv-上傳完整指南)
6. [客戶管理（CRM）](#6-客戶管理crm)
7. [商品成本設定](#7-商品成本設定)
8. [AI 顧問建議](#8-ai-顧問建議)
9. [自動規則引擎](#9-自動規則引擎)
10. [月報產製與寄送](#10-月報產製與寄送)
11. [資料夾自動上傳](#11-資料夾自動上傳)
12. [瀏覽器自動上傳腳本](#12-瀏覽器自動上傳腳本)
13. [系統設定](#13-系統設定)
14. [常見問題 FAQ](#14-常見問題-faq)
15. [故障排除](#15-故障排除)

---

> 📸 **本文檔含截圖佔位符**（`docs/screenshots/*.png`），首次發佈時為空。補截圖方式見 [附錄 B](#附錄-b-如何補截圖)。

---

## 1. 系統概覽

![截圖：首頁全景](docs/screenshots/01-dashboard-overview.png)

### 1.1 系統用途
本系統是蝦皮廣告代操業者的多客戶儀表板，整合：
- 廣告數據視覺化（KPI、ROAS、真實獲利）
- AI 自動建議（基於真實 CSV 數據）
- 客戶 CRM + 月報自動產製
- 多客戶切換與權限隔離

### 1.2 系統架構
```
你的瀏覽器
    ↓ HTTPS（Cloud Run 自動）
    ↓ HTTP Basic Auth
Cloud Run service: shopee-dashboard（asia-east1）
    ├─ FastAPI + uvicorn
    ├─ min/max = 1 instance（保證資料一致性）
    └─ mount GCS bucket → /data（持久化）
```

### 1.3 線上服務網址
```
https://shopee-dashboard-zctlocldga-de.a.run.app
```

### 1.4 資料來源
| 數據類別 | 來源 |
|---|---|
| 廣告 spend / ROAS / CTR / 點擊 | CSV 上傳（蝦皮 Open API 不開放） |
| 客戶資料 / 合約 / 月費 | 系統內 CRM 管理 |
| 商品成本 / 運費 / 平台費率 | 系統內 profit_config |
| AI 建議 | OpenAI GPT-4o（沒設 key 用規則式 fallback） |
| 月報寄送 | Gmail SMTP（沒設 key 用 mock） |
| Google Trends 熱搜 | Google Trends RSS（無需金鑰） |

---

## 2. 登入與帳號

![截圖：登入彈窗](docs/screenshots/02-login-prompt.png)

### 2.1 登入方式
打開 https://shopee-dashboard-zctlocldga-de.a.run.app 後，瀏覽器會跳出帳密輸入框：
- **帳號**：`admin`
- **密碼**：由系統管理員提供（存在 GCP Secret Manager，會定期更換）

### 2.2 查詢目前密碼（管理員）
```bash
gcloud secrets versions access latest \
    --secret=shopee-dashboard-auth-pass \
    --project=order-system-dev-495106
```

### 2.3 更換密碼（管理員）
```bash
echo -n 'NewPassword' | gcloud secrets versions add \
    shopee-dashboard-auth-pass --data-file=- \
    --project=order-system-dev-495106
# Cloud Run 約 1 分鐘內自動拿到新密碼，舊版本自動 disabled
```

### 2.4 登入失敗排查
| 現象 | 原因 | 解法 |
|---|---|---|
| HTTP 401 | 密碼過時 | 用 `gcloud secrets versions access latest` 取最新版 |
| HTTP 503 | Cloud Run cold start | 等 30 秒重試 |
| 瀏覽器不跳帳密框 | 已記憶舊密碼 | 清除瀏覽器密碼或開無痕視窗 |

---

## 3. 介面導覽

![截圖：客戶切換器 + 左側選單](docs/screenshots/03-sidebar-customer-switcher.png)

### 3.1 頂端
- **目前檢視 下拉**：選擇要看哪個客戶（或「所有客戶（合併）」）
- 切換後**所有頁面**同步 filter

### 3.2 左側選單分組

#### 📊 總覽
| 頁面 | 功能 |
|---|---|
| **KPI + 真實獲利** | 整體 spend/revenue/ROAS + 扣完成本後的真實淨利 |
| **帳號總表** | 每個店家的真實數字（已綁客戶 / 未綁列表）|
| **高效時段** | 7 天 × 24 小時熱力圖（按星期粒度，CSV 沒小時資料）|
| **活動行事曆** | 近期上傳紀錄 + 規則觸發紀錄 |

#### 🔧 優化工具
| 頁面 | 功能 |
|---|---|
| **AI 調整建議** | 從真實 CSV 商品逐個產生具體可執行建議 |
| **商品優化追蹤** | 上升商品排行 + 投放前後對比（30 天 vs 60 天前）|
| **商品健康度** | 評分 + 庫存 + 銷量綜合，每商品標「強投/正常/減投/停投」|
| **預算與自動規則** | 預算燒爆預警 + 規則引擎（dry-run / 真執行）|

#### 🌐 流量策略
| 頁面 | 功能 |
|---|---|
| **關鍵字研究** | 真實關鍵字表現 + 版位表現 + Google Trends 今日熱搜 |
| **受眾 + 素材** | 受眾包管理 + AI 廣告文案生成 |
| **競品監控** | 競品商品追蹤（需要手動加） |

#### 👥 客戶與運營
| 頁面 | 功能 |
|---|---|
| **客戶 CRM + 月報** | 客戶清單 + 合約管理 + 月報產製/PDF/寄信 |
| **團隊 + LINE 通知** | 操作員管理 |
| **跨平台整合** | 蝦皮/Momo/PChome 比較（部分 mock） |
| **整合與設定** | CSV 上傳 / 蝦皮 OAuth 綁定 / 資料夾監看 / API key 狀態 |

---

## 4. 每日操作流程

### 4.1 早班流程（10 分鐘）

```
1. 打開 dashboard → 登入
2. 看「KPI + 真實獲利」：昨日花費 / 營收 / ROAS
   - ROAS 低於目標 → 紅色警告
   - 真實淨利為負 → 立刻處理
3. 點「✨ AI 顧問建議」「產生建議」
   - 看「立即動作」3 條
4. 點「商品優化追蹤」
   - ROAS < 1 的商品 → 暫停或降出價
   - ROAS 超標 20% → 加碼預算
5. 切到「預算與自動規則」
   - 點「🔍 試算」看哪些商品會被自動規則命中
   - 確認沒問題 → 「▶ 立即執行」
```

### 4.2 上傳每日 CSV（5 分鐘）

```
1. 蝦皮賣家中心 → 行銷中心 → 我的廣告
2. 商品廣告分頁 → 期間選「昨天」
3. 匯出數據 → 「總體廣告數據」CSV
4. 進 dashboard「整合與設定」→ 拖 CSV 到上傳區
5. 確認綠色徽章「✓ 解析成功 N 筆」
6. 如需關鍵字層級數據 → 匯出時改選「關鍵字/版位層級數據」
```

### 4.3 月底流程（月報寄送）

```
1. 客戶 CRM + 月報頁
2. 點對應客戶「📊 月報」按鈕
3. 確認數據 + 亮點 + 下月計畫
4. 點「📤 寄出月報」
   - 已設 Gmail → 真實寄送
   - 未設 → mock log
5. 寄送紀錄會留在「📋 寄送紀錄」區
```

> 💡 **月報自動排程**：每月 1 號 09:00 自動跑（Cloud Scheduler），所有客戶月報自動寄出。不用手動。

---

## 5. CSV 上傳完整指南

### 5.1 兩種報表類型

| 類型 | 用途 | 影響頁面 |
|---|---|---|
| **總體廣告數據** | 看整體 KPI、商品表現 | KPI / 真實獲利 / 商品健康度 / 月報 |
| **關鍵字/版位層級數據** | 看哪個關鍵字/版位賺錢 | 關鍵字研究頁 |

### 5.2 五種期間選擇（影響數據準確度）

| 期間 | 標示 | 建議 |
|---|---|---|
| **昨天 ~ 昨天**（1 天） | 🟢 最精準 | **每日上傳的最佳選擇**，趨勢圖才會生動 |
| **過去一週**（7 天） | 🟢 可用 | 每週上傳一次也 OK |
| **過去一個月**（30 天） | 🟡 數字膨脹 | 每筆已是 30 天累積，KPI 趨勢無法看 |
| **近三個月**（90 天） | 🔴 不建議 | 系統會警告，建議改成「昨天」 |
| **今天** | 🟢 可用 | 當天監控用 |

### 5.3 蝦皮匯出步驟（完整圖文）

```
1. https://seller.shopee.tw 登入
2. 行銷中心 → 我的廣告
3. 上方分頁：商品廣告 / 新商品廣告 / 賣場廣告 / 品牌廣告 / 直播廣告
   → 選「商品廣告」（最常用）
4. 右上角日期：點下拉選「昨天」（或其他）
5. 右上角「匯出數據」下拉：
   - 「總體廣告數據」← 主要每天用這個
   - 「關鍵字/版位層級數據」← 進階分析用
6. 下載 CSV 到本機
```

### 5.4 上傳到 Dashboard

#### 方法 A：手動拖檔（最簡單）
```
整合與設定 → 廣告報表 CSV 上傳
店家 / 客戶下拉：選對應店家
拖 CSV 到「拖曳 CSV 到此」框
等 1-2 秒看到綠色「✓ 解析成功」
```

#### 方法 B：資料夾自動上傳（參考第 11 章）

#### 方法 C：Tampermonkey 腳本（參考第 12 章）

![截圖：上傳成功 + 報表類型徽章 + 粒度徽章](docs/screenshots/05-upload-success-badges.png)

### 5.5 上傳後的徽章看懂

| 徽章 | 含義 |
|---|---|
| 📊 總體廣告數據 | report_type = overall |
| 🔑 關鍵字/版位層級 | report_type = keyword_placement |
| 🟢 期間 X ~ Y · 1 天 | 最精準 |
| 🟡 N 天彙總 | 數字已疊加，趨勢失真 |
| 🔴 90 天 lifetime | 強烈建議改單日 |
| 「欄位映射 X/Y 已識別」 | 看有沒有未識別欄位 |

### 5.6 重複上傳偵測

系統會自動偵測「同店家 + 同檔名 + 同期間」的重複上傳 → HTTP 409
- UI 跳橘色「⚠️ 重複了」+ 兩個按鈕
- 「取消」→ 不上傳
- 「仍要上傳」→ 數字會被加倍（一般不要選）

### 5.7 期間單位自動 normalize（避免百分比錯誤）

商品成本設定中，**百分比可輸入 0.05（小數）或 5（百分比）**，系統自動偵測：
- `5.5` → 視為 5.5%
- `0.055` → 視為 5.5%（小數）
- 兩者結果一樣

### 5.8 報表分倉系統（v3.4 Phase 1-3）

#### 為什麼要分倉？

之前的痛點：
- 上傳「90 天 lifetime CSV」→ 數字疊加到每天的 KPI → 趨勢圖膨脹失真
- 上傳「關鍵字/版位 CSV」→ 同樣商品的數字被算兩次（總體一次、關鍵字拆解又一次）

→ 系統自動把每筆 row 分到 **兩個維度** 倉裡：

| 維度 | 值 | 說明 |
|---|---|---|
| `report_type` | `overall` | 總體廣告數據（每商品/廣告活動一行） |
|  | `keyword_placement` | 關鍵字/版位層級數據 |
| `granularity` | `daily` | 單一天的數據（最精準）|
|  | `aggregate` | 多日彙總（30 天 / 90 天 lifetime 等）|

#### 主儀表板只吃「總體 + 單日」
KPI / 真實獲利 / 商品健康度 / 預算燃燒 / 趨勢圖 → **只用 `overall + daily` 倉**：
- 不會被關鍵字/版位 CSV 重複加總
- 不會被 90 天 lifetime CSV 污染日期型聚合

#### 多日彙總走「區間總覽」區塊
- KPI 頁主卡片下方有「📅 區間總覽」卡（v3.4 新增）
- 列出所有 `aggregate` 粒度的 CSV，每筆當「**區間總計**」獨立呈現
- 不拆日、不混進日級 KPI

#### 上傳區的「粒度徽章」
上傳結果除了「報表類型徽章」外，還會看到：
- 🟢 **單日資料** — 進主儀表板
- 🟡 **N 日彙總** — 進區間總覽

#### 店家只有彙總沒單日時
KPI 卡會顯示「該店家只有多日彙總資料，請看下方區間總覽」（避免之前誤導的 $0）。

---

## 6. 客戶管理（CRM）

### 6.1 新增客戶
1. 進「客戶 CRM + 月報」頁
2. 點「+ 新增客戶」
3. 填寫：
   - 客戶名稱
   - 聯絡資訊（電話、email、LINE）
   - 服務店家（**shop_ids** — 對應 CSV 上傳時選的店家名）
   - 合約期 + 月費
   - 目標 ROAS / ACoS
   - 結算日

![截圖：CRM 客戶清單 + 新增客戶 modal](docs/screenshots/06-crm-customers.png)

### 6.2 重要：shop_ids 必須跟 CSV 上傳時的「店家名」一致
例如：
- 客戶「科爸好皮」`shop_ids: ["自家3C旗艦店"]`
- CSV 上傳時下拉選「自家3C旗艦店」
- → 系統自動把該 CSV 數據歸到該客戶

不一致就會「客戶月報沒數據」。

### 6.3 編輯/刪除客戶
- 行內點欄位即可編輯
- 刪除前確認沒有相關 CSV 資料（會孤兒化）

---

## 7. 商品成本設定

### 7.1 為什麼要設成本
ROAS 高不等於賺錢！必須扣：
- 商品成本（cost × 訂單數）
- 運費（每單固定 or 營收 %）
- 平台抽成（蝦皮 5.5%）
- 刷卡費（3%）

→ 才知道**真實淨利**。

### 7.2 設定欄位

| 欄位 | 說明 | 範例 |
|---|---|---|
| `cost` | 每件商品實際進貨成本 | 611 元 |
| `shipping` | 每單運費（固定金額） | 60 元 |
| `shipping_rate` | 運費佔營收比率（%）| 5 = 5% |
| `platform_fee_rate` | 平台抽成 | 5.5 = 5.5% |
| `payment_fee_rate` | 刷卡手續費 | 3 = 3% |
| `selling_price` | 商品售價 | 1222 元 |

> ⚠️ `shipping` 跟 `shipping_rate` **二擇一**。如果都設，`shipping_rate` 優先。

### 7.3 設定位置
進「KPI + 真實獲利」頁 → 真實獲利區塊 → 點「⚙️ 商品成本設定」

![截圖：商品成本設定 modal](docs/screenshots/07-product-cost-config.png)

### 7.4 真實獲利公式
```
真實淨利 = 營收
         − 廣告花費
         − (cost × 訂單數)
         − (shipping × 訂單數) OR (營收 × shipping_rate)
         − (營收 × platform_fee_rate)
         − (營收 × payment_fee_rate)
```

---

## 8. AI 顧問建議

### 8.1 兩種模式

| 模式 | 觸發條件 | 品質 |
|---|---|---|
| **OpenAI GPT-4o**（真實 AI） | 環境變數 `OPENAI_API_KEY` 已設 | 深入、客製化、會考量所有 context |
| **規則式 fallback** | 沒設 OPENAI | 基於規則，簡單但有用 |

![截圖：AI 顧問建議卡（診斷/立即動作/本週計畫）](docs/screenshots/08-ai-advisor.png)

### 8.2 產出 3 段建議

```
🔍 診斷（1-3 條）
  - 找出主要問題（淨利、ROAS、商品集中度）

⚡ 立即動作（1-3 條）
  - 今天就該做的具體動作（含商品名、數字）

📅 本週計畫（1-3 條）
  - 本週要做的優化（加碼/降出價/換素材）
```

### 8.3 如何啟用 OpenAI
管理員設定 Secret Manager：
```bash
echo -n 'sk-proj-xxx' | gcloud secrets versions add OPENAI_API_KEY_FBTOOL \
    --data-file=- --project=order-system-dev-495106
```

### 8.4 何時用 AI vs 規則式
- 日常 → 規則式即可（免費、快）
- 重大決策 / 月報前 → 切 AI（給更深入分析）

---

## 9. 自動規則引擎

### 9.1 規則格式
```
metric (roas/ctr/cpc/spend/acos) operator threshold action
```

例如：
- `ROAS < 2.5` → `lower_bid 20%`（降出價 20%）
- `ROAS > 5` → `raise_bid 30%`（加碼預算 30%）
- `ROAS < 1` → `pause`（暫停）

### 9.2 兩種執行模式

| 按鈕 | 行為 |
|---|---|
| 🔍 **試算 (dry-run)** | 看哪些商品會被命中，不執行 |
| ▶ **立即執行** | 真的觸發（目前 mock 寫入 log，等接 Shopee Partner API） |

### 9.3 規則設定位置
「預算與自動規則」頁 → 點「+ 新增規則」

![截圖：自動規則引擎（試算結果）](docs/screenshots/09-rules-engine.png)

---

## 10. 月報產製與寄送

### 10.1 兩種觸發方式

| 方式 | 時機 | 對象 |
|---|---|---|
| **手動** | 隨時 | 單一客戶 |
| **自動排程** | 每月 1 號 09:00 | 所有有資料的客戶 |

### 10.2 月報內容
- 客戶資訊 + 合約期
- 月度 KPI（spend / revenue / ROAS / 訂單）
- 真實獲利分解（含商品成本/運費/抽成）
- Top 3 商品表現
- 每日趨勢圖
- AI 自動產生的「亮點」+「下月計畫」

![截圖：月報 modal（含真實獲利分解 + Top 商品）](docs/screenshots/10-monthly-report.png)

### 10.3 月報下載（PDF）
- 客戶 CRM 頁點「📊 月報」
- Modal 內點「📄 下載 PDF」
- 跳出瀏覽器列印對話框 → 目的地選「儲存為 PDF」

### 10.4 月報寄送

| 寄送方式 | 環境變數 | 寄送結果 |
|---|---|---|
| **Gmail SMTP** | `GMAIL_USER` + `GMAIL_APP_PASSWORD` | 真實寄到客戶 email |
| **Mock** | 沒設環境變數 | 寫入 log，不真寄 |

### 10.5 Gmail App 密碼設定（管理員）
1. 到 https://myaccount.google.com/apppasswords
2. 產一個 App 密碼（16 碼）
3. 設 Secret Manager：
```bash
echo -n 'yourname@gmail.com' | gcloud secrets versions add gmail-user --data-file=-
echo -n 'xxxx xxxx xxxx xxxx' | gcloud secrets versions add gmail-app-password --data-file=-
```

---

## 11. 資料夾自動上傳

### 11.1 用途
不想每天點 dashboard 上傳？把 CSV 拖到固定資料夾，系統自動處理。

### 11.2 設定

```
在「整合與設定」頁 → 資料夾監看 區塊
顯示監看路徑：watch/{店家名}/
```

### 11.3 使用

```
1. 蝦皮匯出 CSV
2. 拖到 watch/自家3C旗艦店/
3. 系統 5 秒內自動掃描 + 解析
4. 成功 → 移到 watch/自家3C旗艦店/processed/
   失敗 → 移到 watch/自家3C旗艦店/failed/
```

### 11.4 支援雲端硬碟同步路徑
可加 Google Drive / OneDrive 同步資料夾當監看目錄：
```
整合與設定 → 資料夾監看 → 新增目錄
貼路徑：G:\我的雲端硬碟\蝦皮報表\
```

---

## 12. 瀏覽器自動上傳腳本

### 12.1 用途
裝 Tampermonkey 後，蝦皮賣家中心匯出 CSV 時**自動上傳到 dashboard**，省手動拖檔。

### 12.2 安裝步驟
1. Chrome 裝 Tampermonkey 擴充套件
2. 開 https://github.com/missparisbeauty/shopee-ad-dashboard/blob/main/shopee-auto-upload.user.js
3. 點 Tampermonkey 圖示 → 安裝
4. 詳細：見 `瀏覽器自動上傳_安裝說明.md`

### 12.3 注意事項
- 公開版（GitHub）**不含密碼**，每次匯出會跳帳密
- 想免登入版：本地手動編輯腳本內的 `BASIC_AUTH` 變數

---

## 13. 系統設定

### 13.1 環境變數（管理員）

| 變數 | 必填 | 來源 |
|---|---|---|
| `BASIC_AUTH_USER` | ✓ | Cloud Run env vars（plain）|
| `BASIC_AUTH_PASS` | ✓ | Secret: `shopee-dashboard-auth-pass` |
| `CRON_TOKEN` | ✓ | Secret: `cron-token`（月報排程用）|
| `OPENAI_API_KEY` | 選 | Secret: `OPENAI_API_KEY_FBTOOL` |
| `GMAIL_USER` | 選 | Secret: `gmail-user` |
| `GMAIL_APP_PASSWORD` | 選 | Secret: `gmail-app-password` |
| `SHOPEE_PARTNER_ID` | 選 | （未來接 Shopee Open API 用）|

### 13.2 GCP 資源

| 資源 | 名稱 |
|---|---|
| Project | `order-system-dev-495106` |
| Cloud Run | `shopee-dashboard`（asia-east1）|
| GCS Bucket | `shopee-dashboard-data-1391087402` |
| Artifact Registry | `cloud-run-source-deploy` |
| Cloud Scheduler | `monthly-reports-job`（每月 1 號）|

### 13.3 CI/CD
- `git push` 到 GitHub `missparisbeauty/shopee-ad-dashboard`
- GitHub Actions 自動：
  1. `ci.yml`：跑 134 個 pytest
  2. `deploy.yml`：測試過 → Cloud Build → Cloud Run

### 13.4 看部署狀態
```bash
gh run list --repo missparisbeauty/shopee-ad-dashboard
```

### 13.5 看 Cloud Run logs
```bash
gcloud run services logs tail shopee-dashboard \
    --project=order-system-dev-495106 --region=asia-east1
```

---

## 14. 常見問題 FAQ

### Q1：為什麼 ROAS 達標但淨利還是負的？
A：ROAS 只看「廣告營收 / 廣告花費」，沒扣商品成本/運費/平台費。**真實淨利**才是衡量是否賺錢的指標。常見原因：
- 運費佔比過高（單價低的商品要設「滿額免運」）
- 商品成本沒設準（用 50% 預設估算不準）

### Q2：上傳 CSV 後 KPI 沒變？
A：檢查順序：
1. 上傳結果有「✓ 解析成功」？
2. 「整合與設定 → 上傳清單」有看到該筆？
3. 店家名跟客戶 shop_ids 一致？
4. 瀏覽器 Ctrl+F5 強制重新整理

### Q3：客戶月報沒數據？
A：客戶 shop_ids 沒對到任何 CSV 上傳的店家。檢查：
- 客戶 CRM 頁 → 該客戶的 shop_ids 欄位
- 跟「整合與設定 → 上傳清單」的「店家」欄比對

### Q4：AI 建議都是規則式，怎麼啟用 OpenAI？
A：管理員到 GCP Secret Manager 設定 `OPENAI_API_KEY_FBTOOL`（見第 8.3）。

### Q5：月報寄出但客戶沒收到？
A：
1. 確認 Gmail App 密碼有效（過期會自動失敗）
2. 看「寄送紀錄」狀態
3. Gmail 帳號的「寄件備份」確認有寄出
4. 客戶 spam 資料夾

### Q6：可以新增多少個客戶？
A：技術上無上限。實際 Cloud Run min/max=1，建議 < 50 個客戶（再多要升級到方案 B：Cloud SQL + 多 instance）。

### Q7：商品成本要設多細？
A：建議**有上廣告的商品都設**。沒設的會用「營收 × 42%」預設估算，誤差可能 ±15%。

### Q8：可以從手機看 dashboard 嗎？
A：可以，但**還沒有 RWD**（行動版排版）。瀏覽用 OK，操作建議桌面/平板。

### Q9：資料安全嗎？
A：
- HTTPS + Basic Auth
- 客戶資料存 GCP 私有 bucket（不公開）
- GitHub repo 公開但**敏感資料都在 gitignore**（customers.json / profit_config.json / 密碼）
- 沒有把客戶 email 給第三方

### Q10：怎麼備份資料？
A：GCS bucket 預設有版本控制，需手動啟用：
```bash
gsutil versioning set on gs://shopee-dashboard-data-1391087402
```

---

## 15. 故障排除

### 15.1 線上服務無法連線
| 狀況 | 檢查 |
|---|---|
| 域名解析錯誤 | 確認網址正確：`shopee-dashboard-zctlocldga-de.a.run.app` |
| HTTP 503 | Cloud Run cold start，等 30 秒重試 |
| HTTP 401 | 密碼過時，查 Secret Manager 最新版 |
| HTTP 500 | Cloud Run logs 看 stack trace |

### 15.2 CSV 上傳失敗
| 錯誤 | 解法 |
|---|---|
| HTTP 409 重複上傳 | 同店家+檔名+期間已上傳過，按「仍要上傳」或改檔名 |
| HTTP 400 無法解析 | CSV 格式不對，看「未識別欄位」確認來源是不是蝦皮匯出 |
| 0 筆解析成功 | header 列沒對到，看 column_map 訊息回報 |

### 15.3 月報沒寄出
| 現象 | 原因 |
|---|---|
| 寄送紀錄狀態 `mock` | Gmail 環境變數沒設 |
| 寄送紀錄狀態 `error: SMTPAuthenticationError` | Gmail App 密碼錯誤或過期 |
| 寄送紀錄狀態 `error: SMTPRecipientsRefused` | 客戶 email 拼錯或不存在 |

### 15.4 自動排程沒跑
```bash
# 手動觸發測試
gcloud scheduler jobs run monthly-reports-job \
    --location=asia-east1 --project=order-system-dev-495106

# 看狀態
gcloud scheduler jobs describe monthly-reports-job \
    --location=asia-east1 --project=order-system-dev-495106
```

### 15.5 看哪個版本部署中
```bash
gcloud run services describe shopee-dashboard \
    --project=order-system-dev-495106 --region=asia-east1 \
    --format='value(status.latestReadyRevisionName,status.url)'
```

---

## 附錄 A：如何產製 PDF 印刷版

### A.1 最簡單：瀏覽器「另存為 PDF」（推薦）

1. 開 https://github.com/missparisbeauty/shopee-ad-dashboard/blob/main/使用說明書.md
2. 鍵盤按 **Ctrl + P**（Windows）或 **Cmd + P**（Mac）
3. **目的地** 選「**儲存為 PDF**」
4. **頁面範圍** 選「全部」
5. **更多設定** → 取消勾「頁首和頁尾」（讓版面乾淨）
6. 按「儲存」

→ 拿到 PDF，可印給操作員。

### A.2 GitHub 自動產製（CI/CD）

倉庫已配置 GitHub Actions（`.github/workflows/build-pdf.yml`）：
- main branch 上的 `使用說明書.md` 有更新時自動跑
- 用 pandoc + xelatex 產 PDF
- 上傳到 GitHub Release `pdf-latest` 標籤
- 下載：https://github.com/missparisbeauty/shopee-ad-dashboard/releases/tag/pdf-latest

> 若還沒看到自動產製，跑 `gh workflow run build-pdf.yml` 手動觸發。

### A.3 本機產製（進階）

需要先裝 pandoc + 中文字型：
```bash
# Windows (Chocolatey)
choco install pandoc miktex

# 產 PDF
pandoc 使用說明書.md -o 使用說明書.pdf \
  --pdf-engine=xelatex \
  -V CJKmainfont="Microsoft JhengHei" \
  -V geometry:margin=2cm
```

---

## 附錄 B：如何補截圖

### B.1 截圖檔放哪
`docs/screenshots/` 目錄（已 commit 但是空的）

### B.2 命名規則
照本說明書內 markdown 圖片佔位符的檔名，例如：
- `01-dashboard-overview.png`
- `02-login-prompt.png`
- `03-sidebar-customer-switcher.png`
- `05-upload-success-badges.png`
- `06-crm-customers.png`
- `07-product-cost-config.png`
- `08-ai-advisor.png`
- `09-rules-engine.png`
- `10-monthly-report.png`

### B.3 推薦截圖工具
| 工具 | 平台 | 優點 |
|---|---|---|
| **Win + Shift + S** | Windows | 內建，圈選即截 |
| **ShareX** | Windows | 自動命名、加標註 |
| **Cmd + Shift + 4** | Mac | 內建，圈選即截 |
| **Lightshot** | 跨平台 | 截後直接加箭頭/文字 |

### B.4 截圖規範
- 格式：**PNG**（無損）
- 寬度：建議 **1200-1600px**（太大文件會臃腫）
- 內容：**只截重點區塊**，不要整個瀏覽器視窗
- 隱私：**模糊掉真實客戶名稱 / email / shop_id 數字**
- 標註：箭頭/紅框標重點處（用 ShareX 或 Lightshot 即可）

### B.5 push 上 GitHub
```bash
git add docs/screenshots/*.png
git commit -m "docs: 補使用說明書截圖"
git push
```

→ Markdown 內的 `![截圖：xxx](docs/screenshots/xxx.png)` 自動 render 出圖。

---

## 附錄 C：相關文件
- `HANDOFF.md` — 開發交接（含密碼，不公開）
- `蝦皮代操儀表板_專案紀錄.md` — 完整功能說明
- `功能真實性對照表.md` — 哪些頁面是真實 vs Mock
- `README-DEPLOY.md` — GCP 部署指南
- `瀏覽器自動上傳_安裝說明.md` — Tampermonkey 腳本
- `developlog.md` — 開發日誌

### 技術支援
- GitHub Issues：https://github.com/missparisbeauty/shopee-ad-dashboard/issues
- 開發者：missparisbeauty

---

> 本說明書版本 v3.4 · 最後更新 2026-05-21
