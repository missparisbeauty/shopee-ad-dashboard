# 蝦皮廣告代操儀表板 — 專案紀錄 v3.0

> 更新日期：2026-05-03 | 作者：Claude（代筆）| 版本：v3.0（**真實數據貫通版**）
>
> 重大里程碑：從 v2.0 的「Mock Demo」演進到 v3.0 的「真實 CSV 數據貫通整個儀表板」

---

## 🚀 30 秒接手指南（給未來重啟對話用）

**把這份 .md 整份貼給 Claude，說一句**：
> 讀完 `蝦皮代操儀表板_專案紀錄.md`，繼續做這個專案

Claude 會立即知道：
- 所有功能在哪、每個檔案的責任
- 如何啟動 server、設環境變數
- 哪些功能已完成、哪些待辦
- 真實數據怎麼進系統、怎麼影響每個頁面

---

## 啟動方法

```bash
cd "C:\Users\winuser\Desktop\蝦皮代操儀表板"
python server.py     # 啟動於 http://localhost:8765
```

server 啟動時會自動：
- 啟用 csv_watcher（每 5 秒掃描 watch/ 資料夾）
- mock 模式（沒設環境變數時）

---

## v2.0 → v3.0 完成的事

### ✅ 1. CSV 解析與資料管線（核心）

**新增模組**：
| 檔案 | 責任 |
|------|------|
| `ad_report_parser.py` | 蝦皮 CSV 解析（中英欄位 60+ 別名、編碼自動偵測、千分號/百分號清洗、日期 8 種格式、摘要列元資料抽取） |
| `ad_data_store.py` | 解析結果持久化到 `ad_reports.json`，提供 7 種聚合查詢（KPI/profit/products/trend/budget/lifetime/...） |

**支援格式**：
- ✅ 蝦皮「按日明細」報表
- ✅ 蝦皮「總體 lifetime 報表」（自動偵測 + 警告 + 從「期間」摘要抽日期）
- ✅ 編碼：utf-8-sig / utf-8 / big5 / cp950 / gb18030
- ✅ 分隔符：comma / tab / semicolon

### ✅ 2. 真實數據貫通 7 大頁面

| 頁面 | 真實 CSV 影響項目 |
|------|------------------|
| 首頁 KPI 總覽 | spend/revenue/roas/ctr/cpm + delta vs 上期 |
| 真實獲利分析 | 用 profit_config 的 cost × orders 算淨利 |
| 商品健康度 | sales_30d 用真實 orders；rating/stock 估算 |
| 預算燃燒預警 | 用 7 日均花費 × 1.2 當預算；最近一天當「今日」 |
| Top 商品 | 按 product_id 聚合排序 |
| 客戶月報 | 依 customer.shop_ids filter，含 profit/top_products/trend |
| 趨勢圖 | 每日 spend/revenue 折線 |

每個都支援 `?source=auto/mock/real&shop=xxx` 切換，前端有綠/灰徽章顯示當前來源。

### ✅ 3. 業務工具

| 工具 | 位置 | 說明 |
|------|------|------|
| **多客戶切換器** | 全域頂端 sticky | 6 個客戶下拉切換、URL 同步 (`?customer=CU0`)、所有頁面同步 filter |
| **profit_config 編輯 UI** | 首頁真實獲利下方（可摺疊）| 行內編輯成本/運費/平台費率，按 Tab 即存；CSV 中尚未設定的商品自動列出 + 「+ 套用建議成本」 |
| **月報 PDF 下載** | 月報 modal「📄 下載 PDF」| 觸發瀏覽器列印對話框另存 PDF；自訂 print CSS 隱藏 UI；自動命名 `{客戶名}_月報_{月份}.pdf` |

### ✅ 4. 自動化

**新增模組**：
| 檔案 | 責任 |
|------|------|
| `csv_watcher.py` | 監看 watch/ 與額外目錄（GDrive/OneDrive 同步路徑），新檔自動解析+移到 processed/ 或 failed/ |
| `rules_engine.py` | 規則對真實商品評估 — `metric (roas/ctr/cpc/spend/acos/orders) operator threshold action` |

**功能**：
- 5 秒週期自動掃描多個監看目錄
- UI 可動態新增 / 移除目錄（支援 GDrive 同步路徑）
- 規則引擎：`/rules/preview` (dry-run) + `/rules/run` (真執行)，顯示具體命中商品

### ✅ 5. 三個外部整合（雙軌：mock + real 自動切換）

| 整合 | 模組 | 環境變數 | 沒設定的行為 |
|------|------|---------|-------------|
| **SendGrid** 月報寄送 | `email_client.py` | `SENDGRID_API_KEY` + `SENDGRID_FROM_EMAIL` | mock 模式（記 log 不寄信） |
| **Shopee Open API** 商品/訂單同步 | `shopee_client.py` | `SHOPEE_PARTNER_ID` + `SHOPEE_PARTNER_KEY` | mock 模式（產假商品） |
| **Claude AI 顧問** 廣告優化建議 | `ai_advisor.py` | `ANTHROPIC_API_KEY`（可 + `ANTHROPIC_MODEL`） | 規則式 fallback（也會產診斷+動作+本週計畫） |

**設了環境變數立刻啟用真實**，不用改 code。

---

## 目錄結構（v3.0）

```
蝦皮代操儀表板/
├── server.py                 # FastAPI 主入口（~1500 行）
├── demo.html                 # 單頁前端（~3500 行）
├── api.js                    # 統一 API 入口
├── requirements.txt
│
├── # ── 核心模組 ──
├── ad_report_parser.py       # CSV 解析 + 元資料抽取
├── ad_data_store.py          # 持久化 + 7 種聚合查詢
├── csv_watcher.py            # 多目錄資料夾監看
├── rules_engine.py           # 自動規則引擎
├── ai_advisor.py             # Claude AI 顧問（含 rule_based fallback）
├── email_client.py           # SendGrid 月報寄送
├── shopee_client.py          # Shopee Open API（mock+real）
│
├── # ── 資料持久化 JSON ──
├── ad_reports.json           # 解析後的 row + upload meta（v3.0 主資料庫）
├── customers.json            # 客戶 CRM
├── profit_config.json        # 商品成本/運費/平台費率
├── rules.json                # 自動規則
├── snapshots.json            # 每週快照
├── audiences.json            # 受眾包
├── competitors.json          # 競品監控
├── shopee_connections.json   # OAuth 綁定
├── shopee_products.json      # 從 Open API 同步的商品
├── notify_config.json        # LINE Notify 設定
├── report_log.json           # 月報寄送紀錄
├── adjustments.json          # 待處理出價/預算調整
├── team.json                 # 團隊成員
├── uploads_index.json        # 上傳索引（向後相容）
├── watcher_state.json        # csv_watcher 狀態（含 extra_watch_dirs）
│
├── # ── 監看資料夾 ──
└── watch/                    # 主監看根目錄
    ├── README.txt            # 使用說明
    ├── S001/                 # 子資料夾 = shop name
    │   ├── *.csv             # 待處理
    │   ├── processed/        # 處理成功（自動加時間戳）
    │   └── failed/           # 處理失敗（保留方便排查）
    └── 自家3C旗艦店/
```

---

## API 端點（80+ 個）

### 核心：CSV / KPI / 真實獲利
```
POST   /api/v1/upload/ad-report               上傳 CSV（自動解析）
GET    /api/v1/uploads                        上傳清單（含類型/期間 metadata）
DELETE /api/v1/uploads/{id}
GET    /api/v1/ad-data/status                 真實資料狀態
GET    /api/v1/ad-data/products               商品聚合
GET    /api/v1/kpi?source=auto&shop=          KPI（mock/real 切換）
GET    /api/v1/kpi/trend?source=auto&shop=
GET    /api/v1/profit/summary?source=auto     真實獲利
GET    /api/v1/profit/config                  + missing_in_csv 自動建議
PUT    /api/v1/profit/config/{pid}
DELETE /api/v1/profit/config/{pid}
GET    /api/v1/products/health-scores?source= 商品健康度
GET    /api/v1/budget/pacing?source=          預算燃燒
```

### 自動化
```
POST   /api/v1/watcher/start | stop | scan-now
GET    /api/v1/watcher/status                 含多 root + 待處理數
POST   /api/v1/watcher/dirs/add  {path}       新增監看目錄
POST   /api/v1/watcher/dirs/remove
POST   /api/v1/rules/preview                  dry-run
POST   /api/v1/rules/run?dry_run=             實際執行
```

### 整合
```
GET    /api/v1/email/status
POST   /api/v1/reports/send                   SendGrid 寄月報（mock+real）

GET    /api/v1/shopee/status
POST   /api/v1/shopee/products/sync?shop_id=  同步商品（mock+real）
GET    /api/v1/shopee/products

GET    /api/v1/ai/status
POST   /api/v1/ai/insights {customer_id, period}  AI 顧問建議
```

### 客戶 / 月報
```
GET    /api/v1/customers
POST   /api/v1/customers
DELETE /api/v1/customers/{cid}
GET    /api/v1/reports/monthly/{customer_id}?source=auto  含真實 profit + top products + trend
GET    /api/v1/reports/log
```

---

## 環境變數（之後啟用真實 API 用）

```bash
# 全部都是「沒設就 mock，設了就 real」自動切換
ANTHROPIC_API_KEY=sk-ant-...                  # AI 顧問
ANTHROPIC_MODEL=claude-sonnet-4-5             # 可選

SENDGRID_API_KEY=SG....                       # 月報真實寄送
SENDGRID_FROM_EMAIL=you@verified.com

SHOPEE_PARTNER_ID=2001234                     # 蝦皮商品/訂單
SHOPEE_PARTNER_KEY=...
SHOPEE_API_HOST=https://partner.shopeemobile.com  # 預設值
```

---

## 真實業務流程（v3.0 設定後的日常）

```
代操業者每天的流程：
   ┌───────────────────────────────────────────────────────────┐
   │ 1. 蝦皮賣家中心 → 廣告報表 → 匯出昨日 CSV               │
   │    (期間設「單一天」避免多日彙總污染)                  │
   │                                                          │
   │ 2. 把 CSV 拖到 watch/{shop_name}/ 或 GDrive 同步資料夾   │
   │    (5 秒內自動處理，移到 processed/，併入儀表板)        │
   │                                                          │
   │ 3. 開 dashboard → 切到對應客戶                           │
   │    → 看 KPI / 真實獲利 / 商品健康度 / 預算燃燒          │
   │    → 點「✨ 產生 AI 建議」拿 3 段優化方案               │
   │                                                          │
   │ 4. 按試算規則 → 看哪些商品該降出價/暫停                  │
   │                                                          │
   │ 5. 月底點「📄 月報 PDF」→ 寄給客戶（SendGrid）          │
   └───────────────────────────────────────────────────────────┘
```

---

## 蝦皮 Open API 現實限制

| 蝦皮 Open API 提供的 | 蝦皮**不**提供的 |
|----------|----------|
| ✅ 商品列表（rating/stock/reviews） | ❌ **廣告花費 / ROAS / CTR / 點擊** |
| ✅ 訂單明細 | ❌ **廣告報表** |
| ✅ 物流/退款/聊聊 | ❌ 投放優化建議 |

**廣告數據只能靠 CSV 上傳**（蝦皮策略性封閉）。我們系統用「混合策略」：
- 廣告數據 → CSV 自動上傳（v3.0 已完成）
- 商品/訂單 → Shopee Open API（v3.0 stub 已備好，等 partner_id）

---

## 已修復的關鍵 Bug

1. ✅ shop label 中文「自家3C旗艦店」被嚴格白名單擋掉 → 改 `_safe_label` 允許中文
2. ✅ `api.uploadAdReport` 沒檢查 `r.ok`，4xx 時 j.data=undefined → 補 detail 解析
3. ✅ 蝦皮真實 CSV 多列被同一 std field 對應（銷售金額/直接銷售金額/Vouchered Sales 都對 revenue），後面 0 蓋前面非 0 → 改「保留第一個有值的」
4. ✅ 蝦皮 lifetime 報表沒按日切（date=None）→ parser 從第 6 列「期間」摘要抽 end_date 當代表日
5. ✅ aggregate_products 包含 `product_id="-"` 等無效值 → 過濾
6. ✅ ROI < 1 不等於虧損（淨利可能仍正）→ UI 改用 `net_profit > 0` 判斷

---

## 待辦清單（v3.0 → v4.0 規劃）

### 短期（1-2 天）
- [ ] 「快照比較」接真實 CSV（每週快照 → 兩週對比 ROAS 變化）
- [ ] 月報「本月亮點/下月計畫」改用 ai_advisor 動態生成（取代 hardcoded rule）
- [ ] 加 README.md（給未來協作者快速上手）

### 中期（1 週）
- [ ] 客戶儀表板分享連結（簡單 token-based 授權，客戶可登入只看自己）
- [ ] 「按日級別」KPI 趨勢的「diff 邏輯」（避免 user 上傳重疊期間造成重複加總）
- [ ] 寫單元測試（pytest）— parser、aggregations、rules engine

### 長期（正式部署）
- [ ] 從 JSON 遷移至 GCP Firestore
- [ ] JWT 認證中介層（三角色：customer/admin/operator）
- [ ] GCP Cloud Run 部署
- [ ] Cloud Scheduler 月底自動觸發月報寄送
- [ ] 實際申請 Shopee Partner ID（5-10 工作天審核）
- [ ] 實際申請 SendGrid（5 分鐘）

### 不做（決策紀錄）
- ❌ **爬蟲自動登入賣家中心** — 違反蝦皮 ToS、客戶帳號可能被封、設備指紋難繞、密碼存儲風險
- ❌ **LINE Notify** — 已 EOL（2025/3/31 結束服務）
- ❌ **WeasyPrint server-side PDF** — Windows 需 GTK runtime，user 安裝門檻高；現用 window.print() + 美化 print CSS 取代

---

## 關鍵設計決策

| 決策 | 理由 |
|------|------|
| Port 8765 | 避免與 fb-tool（8000）衝突 |
| 單一 HTML 檔 | Demo 階段方便，不需建置工具；之後拆分容易 |
| JSON 持久化 | Demo 無需 DB，架構設計為可換 Firestore |
| `?source=auto/mock/real` 三模式 | 開發/Demo/正式環境無痛切換 |
| 雙軌 mock+real（SendGrid/Shopee/Claude）| 任何 API 沒設定都有 fallback，不會 server crash |
| 多目錄監看（GDrive 友善）| 操作員不用登入網站，雲端同步路徑直接拖檔 |
| `_safe_label` 允許中文 shop name | 真實使用情境（賣場名都中文） |
| 客戶切換器頂端 sticky | 代操業者每天切看不同客戶，UX 必須 |
| AI 顧問規則式 fallback | 沒 API key 也有用，不浪費 user 時間設定 |

---

## 下次接手 Claude 時：常見任務範例

- **新增解析支援的欄位**：編輯 `ad_report_parser.py` 的 `COLUMN_ALIASES`
- **改商品成本**：UI 點「⚙️ 商品成本設定」展開，行內編輯
- **新增監看目錄**：UI 點「+ 新增監看目錄」貼路徑（支援 GDrive/OneDrive）
- **看真實 KPI**：選對應客戶 → 上方 KPI 自動 filter
- **產 AI 建議**：點「✨ 產生建議」（沒 API key 走規則式 fallback）
- **寄月報**：CRM 頁 → 客戶月報 → 「📤 寄出月報」（沒 SendGrid 是 mock）
- **下載月報 PDF**：月報 modal → 「📄 下載 PDF」（瀏覽器列印對話框另存）

---

*最後更新：2026-05-03 | server 版本：v3.0 | 端點：80+ | 模組：8 個 .py | 完整真實 CSV 數據貫通*
