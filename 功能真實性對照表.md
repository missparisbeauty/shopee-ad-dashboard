# 功能 vs 真實 CSV 數據 對照表

更新：2026-05-05

---

## ✅ 100% 真實（完全用 CSV）

| 頁面 | 端點 | 數據來源 |
|------|------|---------|
| **首頁 KPI 總覽** | `/api/v1/kpi` | CSV row 聚合（spend/revenue/roas/ctr/cpm + delta） |
| 首頁趨勢圖 | `/api/v1/kpi/trend` | 每日 spend/revenue 折線 |
| **真實獲利分析** | `/api/v1/profit/summary` | KPI × profit_config 算淨利 |
| 真實獲利下方「Top 商品」 | `/api/v1/ad-data/products` | 按 product_id 聚合排序 |
| 商品成本設定面板 | `/api/v1/profit/config` | CSV 商品清單 + 自動建議成本 |
| **AI 廣告顧問建議** | `/api/v1/ai/insights` | 全部 dashboard 數據餵給 Claude（沒 key 走規則式 fallback） |
| **帳號總表** | `/api/v1/accounts` | 每個 shop 的真實 spend/revenue/roas |
| **商品優化追蹤 - 上升商品** | `/api/v1/products/rising` | 按 ROAS×訂單 算分數 + 建議 |
| **商品優化追蹤 - 投放後優化** | `/api/v1/products/optimized` | 近 30 天 vs 之前 30 天比較 |
| **商品健康度** | `/api/v1/products/health-scores` | sales_30d 用真實訂單；rating/stock 估算 |
| **預算燒爆預警** | `/api/v1/budget/pacing` | 7 日均花費 × 1.2 當預算 |
| **預算警示** | `/api/v1/budget/alerts` | 從 pacing 衍生 |
| **自動規則引擎** | `/api/v1/rules/run`、`/preview` | 對真實商品評估條件 |
| **客戶月報** | `/api/v1/reports/monthly/{cid}` | 含 KPI/profit/Top 商品/趨勢/AI 亮點 |
| **活動行事曆** | `/api/v1/events` | 近期 CSV 上傳 + 規則觸發紀錄 |
| **整合與設定 - 上傳清單** | `/api/v1/uploads` | 含類型（單日/N 日彙總）、期間、筆數 |
| **資料夾自動上傳監看** | `/api/v1/watcher/*` | 多目錄、自動處理、processed/failed |

---

## ⚠️ 仍 Mock / 部分真實

| 頁面 | 狀態 | 為什麼 |
|------|------|------|
| **高效時段熱力圖** | ❌ Mock | 蝦皮 CSV 沒「小時」維度 |
| **競品監控（蝦皮搜尋）** | ❌ 暫不可用 | 蝦皮 v4 公開 API 已測試被擋（純 HTTP / cloudscraper 都 403）。要等 Shopee Open API |
| **關鍵字研究 - 今日熱搜** | ✅ **真實**（Google Trends RSS） | 已串接 |
| **關鍵字研究 - 探索工具** | ❌ Mock | 需要 pytrends 套件 |
| **AI 調整建議**（左欄） | ✅ **真實** | 從 CSV 商品逐個產生具體建議 |

### 已測試「蝦皮 v4 公開 API」結果（2026-05-05）
- ❌ 純 urllib：HTTP 403
- ❌ cloudscraper 1.2.71：HTTP 403
- 結論：蝦皮 2024+ 強化反爬蟲，這條路死路。
- **正確解法**：申請 [Shopee Open API Partner](https://open.shopee.com)，5-10 工作天審核，拿到後設環境變數 `SHOPEE_PARTNER_ID` + `SHOPEE_PARTNER_KEY`。

---

## 🔧 用戶自建（不來自 CSV）

| 頁面 | 來源 |
|------|------|
| **客戶 CRM** | `customers.json`，目前只有 CU0「科爸好皮」 |
| **受眾 + 素材** | `audiences.json`，目前空 |
| **競品監控對象** | `competitors.json`，目前空 |
| **團隊管理** | `team.json`，預設值 |
| **自動規則** | `rules.json`，目前空（你之後要建幾條規則） |
| **每週快照** | `snapshots.json`，目前空（每週手動或自動拍） |
| **商品成本設定** | `profit_config.json`，目前空（建議從 dashboard UI 行內編輯） |

---

## 🔌 外部 API 整合（mock + real 雙軌）

| 整合 | 狀態 | 啟用真實要做什麼 |
|------|------|----------------|
| **Claude AI** | mock（規則式 fallback） | 設 `ANTHROPIC_API_KEY` 環境變數 |
| **SendGrid Email** | mock（記 log 不寄信） | 設 `SENDGRID_API_KEY` + `SENDGRID_FROM_EMAIL` |
| **Shopee Open API** | mock（產假商品） | 設 `SHOPEE_PARTNER_ID` + `SHOPEE_PARTNER_KEY`（蝦皮審核 5-10 天） |

---

## 📊 你目前的真實資料（截至 2026-05-05）

```
客戶數：1（科爸好皮 BODY&FACE）
上傳 CSV：3 筆
  - 自家3C旗艦店：2 個檔（含一份 2/3-5/3 共 90 天 lifetime）
  - 自家美妝專櫃：1 個檔（90 天 lifetime）
真實商品：2 個
  - 26981673677 BODY&FACE 美白 修護乳165g
  - 41106223054 BODY&FACE 靈芝 外囊泡 強化髮根精華 30mL
KPI（科爸好皮 月）：
  - 總花費 NT$21,500
  - 總營收 NT$101,310
  - ROAS 4.71（目標 3.5，超標）
  - 真實淨利 NT$45,345
```

---

## 🎯 開始正式使用的 5 個建議

1. **上傳真實 CSV 改用「單日」匯出**（不要 30/90 天 lifetime） — 期間設「昨天 ~ 昨天」即可。
   累積一週 → 7 個資料點 → KPI 趨勢圖才會生動。

2. **設定真實商品成本** — 進首頁「⚙️ 商品成本設定」，把 26981673677 跟 41106223054 的：
   - cost：實際進貨成本
   - shipping：你實際付的運費
   - shipping_rate：如果是按比例付（0 = 不用 %）
   - platform_fee_rate：保留 0.055（蝦皮抽 5.5%）
   - payment_fee_rate：保留 0.03（金流刷卡費）

3. **建幾條規則**（預算與自動規則頁）：
   - 「ROAS < 2.5 → 降出價 20%」
   - 「ROAS > 5 → 加碼預算 30%」

4. **「自家美妝專櫃」shop 還沒綁客戶** — 新增一個客戶，shop_ids 填 `自家美妝專櫃`，之後切到該客戶能單獨看。

5. **每週快照** — 點首頁「📦 建立本週快照」按鈕，累積後就有對比。

---

*本對照表自動產生，最後更新 2026-05-05*
